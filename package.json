{
  "name": "inam-karim-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix client",
    "dev": "npm run start --prefix client",
    "build": "npm run build --prefix client",
    "start": "npx serve -s client/build"
  }
}
